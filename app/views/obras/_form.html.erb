<%= form_for(@obra) do |f| %>
  <% if @obra.errors.any? %>
    <div id="error_explanation">
       <div class="alert alert-danger">
      <h2><%= pluralize(@obra.errors.count, "error") %> prohibited this obra from being saved:</h2>

      <ul>
      <% @obra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
      </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :importe %><br>
    <%= f.text_field :importe %>
  </div>
  <div class="field">
    <%= f.label :plazo %><br>
    <%= f.text_field :plazo, :disabled =>true %>
  </div>
  <div class="field">
    <%= f.label :fechainicial %><br>
    <%= f.date_select :fechainicial %>
  </div>
  <div class="field">
    <%= f.label :fechafinal %><br>
    <%= f.date_select :fechafinal %>
  </div>
  <div class="field">
    <%= f.label :empresa_id %><br>
    <%= f.collection_select( :empresa_id, Empresa.all, :id, :nombre, { :prompt => true, :selected => @obra.empresa_id} )%>
    
  </div>
  <div class="actions">
    <%= f.submit , class: "btn btn-primary"%>
  </div>
<% end %>
